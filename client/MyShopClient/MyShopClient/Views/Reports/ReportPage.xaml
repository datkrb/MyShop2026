<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MyShopClient.Views.Reports.ReportPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyShopClient.Views.Reports"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:lvc="using:LiveChartsCore.SkiaSharpView.WinUI"
    xmlns:models="using:MyShopClient.Models"
    xmlns:helpers="using:MyShopClient.Helpers"
    mc:Ignorable="d">

    <Page.Resources>
        <helpers:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <helpers:InverseBoolToVisibilityConverter x:Key="BoolToVisibilityInverseConverter"/>
    </Page.Resources>

    <Grid Padding="24" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header & Filters -->
        <StackPanel Grid.Row="0" Spacing="16" Margin="0,0,0,24">
            <TextBlock Text="Reports &amp; Analytics" Style="{StaticResource TitleTextBlockStyle}"/>

            <StackPanel Orientation="Horizontal" Spacing="12">
                <CalendarDatePicker Date="{x:Bind ViewModel.StartDate, Mode=TwoWay}" Header="Start Date"/>
                <CalendarDatePicker Date="{x:Bind ViewModel.EndDate, Mode=TwoWay}" Header="End Date"/>
                
                <ComboBox Header="View By" SelectedItem="{x:Bind ViewModel.SelectedReportType, Mode=TwoWay}" ItemsSource="{x:Bind ViewModel.ReportTypes}" MinWidth="120"/>
                
                <ComboBox Header="Product Category" ItemsSource="{x:Bind ViewModel.Categories, Mode=OneWay}" SelectedItem="{x:Bind ViewModel.SelectedCategory, Mode=TwoWay}" MinWidth="150"/>

                <Button Content="Load Data" Command="{x:Bind ViewModel.LoadDataCommand}" VerticalAlignment="Bottom" Style="{StaticResource AccentButtonStyle}"/>
                <ProgressRing IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}" VerticalAlignment="Bottom"/>
            </StackPanel>
        </StackPanel>

        <!-- Content -->
        <ScrollViewer Grid.Row="1">
            <StackPanel Spacing="24">
                
                <!-- Revenue Section -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Revenue Chart -->
                    <Grid Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="12" Height="350" Margin="0,0,12,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="Revenue &amp; Trends" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,0,0,8"/>
                        <lvc:CartesianChart Grid.Row="1" Series="{x:Bind ViewModel.RevenueSeries, Mode=OneWay}" XAxes="{x:Bind ViewModel.RevenueXAxes, Mode=OneWay}"/>
                    </Grid>
                    
                    <!-- Revenue Summary Table -->
                    <Grid Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16" Height="350">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="Revenue Summary" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,0,0,12"/>
                        <StackPanel Grid.Row="1" Spacing="12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Total Revenue" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Grid.Column="1" Text="{x:Bind ViewModel.TotalRevenue, Mode=OneWay}" FontWeight="SemiBold"/>
                            </Grid>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Highest Day" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Grid.Column="1" Text="{x:Bind ViewModel.HighestRevenueDay, Mode=OneWay}" FontWeight="SemiBold"/>
                            </Grid>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Average/Day" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Grid.Column="1" Text="{x:Bind ViewModel.AverageRevenue, Mode=OneWay}" FontWeight="SemiBold"/>
                            </Grid>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Data Points" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Grid.Column="1" Text="{x:Bind ViewModel.RevenueDataPoints, Mode=OneWay}" FontWeight="SemiBold"/>
                            </Grid>
                        </StackPanel>
                    </Grid>
                </Grid>

                <!-- Top 5 Products Section -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Products Chart -->
                    <Grid Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="12" Height="350" Margin="0,0,12,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="Top 5 Products (by Quantity)" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,0,0,8"/>
                        <lvc:CartesianChart Grid.Row="1" Series="{x:Bind ViewModel.ProductSalesSeries, Mode=OneWay}" XAxes="{x:Bind ViewModel.ProductSalesXAxes, Mode=OneWay}" TooltipPosition="Top"/>
                    </Grid>
                    
                    <!-- Products Summary Table -->
                    <Grid Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16" Height="350">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="Top Products" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,0,0,12"/>
                        <ListView Grid.Row="1" ItemsSource="{x:Bind ViewModel.TopProductsList, Mode=OneWay}" SelectionMode="None">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Grid Padding="0,4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Name}" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Grid.Column="1" Text="{Binding Quantity}" FontWeight="SemiBold" Margin="8,0,0,0"/>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Grid>
                </Grid>

                <!-- Profit Section -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Profit Chart -->
                    <Grid Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="12" Height="350" Margin="0,0,12,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="Profit vs Cost" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,0,0,8"/>
                        <lvc:PieChart Grid.Row="1" Series="{x:Bind ViewModel.ProfitSeries, Mode=OneWay}" LegendPosition="Right"/>
                    </Grid>
                    
                    <!-- Profit Summary Table -->
                    <Grid Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16" Height="350">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="Profit Summary" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,0,0,12"/>
                        <StackPanel Grid.Row="1" Spacing="12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Total Revenue" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Grid.Column="1" Text="{x:Bind ViewModel.ProfitRevenue, Mode=OneWay}" FontWeight="SemiBold"/>
                            </Grid>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Total Cost" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Grid.Column="1" Text="{x:Bind ViewModel.ProfitCost, Mode=OneWay}" FontWeight="SemiBold"/>
                            </Grid>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Net Profit" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Grid.Column="1" Text="{x:Bind ViewModel.NetProfit, Mode=OneWay}" FontWeight="SemiBold" Foreground="Green"/>
                            </Grid>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Profit Margin" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Grid.Column="1" Text="{x:Bind ViewModel.ProfitMargin, Mode=OneWay}" FontWeight="SemiBold"/>
                            </Grid>
                        </StackPanel>
                    </Grid>
                </Grid>
                
                <!-- KPI & Commission Section -->
                <Grid Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Header with filters -->
                    <Grid Grid.Row="0" Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="KPI &amp; Commission" Style="{StaticResource SubtitleTextBlockStyle}"/>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="12">
                            <ComboBox Header="Year" ItemsSource="{x:Bind ViewModel.KpiYears, Mode=OneWay}" SelectedItem="{x:Bind ViewModel.SelectedKpiYear, Mode=TwoWay}" MinWidth="100"/>
                            <ComboBox Header="Month" ItemsSource="{x:Bind ViewModel.KpiMonths, Mode=OneWay}" SelectedItem="{x:Bind ViewModel.SelectedKpiMonth, Mode=TwoWay}" MinWidth="100" PlaceholderText="All"/>
                            <Button Content="Load KPI" Command="{x:Bind ViewModel.LoadKpiCommand}" VerticalAlignment="Bottom" Style="{StaticResource AccentButtonStyle}"/>
                        </StackPanel>
                    </Grid>
                    
                    <!-- Admin View: DataGrid with all employees' KPI -->
                    <Grid Grid.Row="1" Visibility="{x:Bind ViewModel.IsAdmin, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                        <ListView ItemsSource="{x:Bind ViewModel.KpiItems, Mode=OneWay}" SelectionMode="None" MaxHeight="300">
                            <ListView.HeaderTemplate>
                                <DataTemplate>
                                    <Grid Padding="8" Background="{ThemeResource SystemControlBackgroundListLowBrush}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="2*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="1.2*"/>
                                            <ColumnDefinition Width="0.8*"/>
                                            <ColumnDefinition Width="1.5*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="Username" FontWeight="SemiBold"/>
                                        <TextBlock Grid.Column="1" Text="Role" FontWeight="SemiBold"/>
                                        <TextBlock Grid.Column="2" Text="Orders" FontWeight="SemiBold" TextAlignment="Right"/>
                                        <TextBlock Grid.Column="3" Text="Revenue" FontWeight="SemiBold" TextAlignment="Right"/>
                                        <TextBlock Grid.Column="4" Text="Rate" FontWeight="SemiBold" TextAlignment="Right"/>
                                        <TextBlock Grid.Column="5" Text="Commission" FontWeight="SemiBold" TextAlignment="Right"/>
                                    </Grid>
                                </DataTemplate>
                            </ListView.HeaderTemplate>
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="models:KpiSalesItem">
                                    <Grid Padding="8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="2*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="1.2*"/>
                                            <ColumnDefinition Width="0.8*"/>
                                            <ColumnDefinition Width="1.5*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{x:Bind User.Username}"/>
                                        <TextBlock Grid.Column="1" Text="{x:Bind User.Role}"/>
                                        <TextBlock Grid.Column="2" Text="{x:Bind Orders}" TextAlignment="Right"/>
                                        <TextBlock Grid.Column="3" Text="{x:Bind RevenueDisplay}" TextAlignment="Right"/>
                                        <TextBlock Grid.Column="4" Text="{x:Bind CommissionRateDisplay}" TextAlignment="Right"/>
                                        <TextBlock Grid.Column="5" Text="{x:Bind CommissionDisplay}" TextAlignment="Right" Foreground="Green" FontWeight="SemiBold"/>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Grid>
                    
                    <!-- Sale User View: Own KPI Card -->
                    <Grid Grid.Row="2" Visibility="{x:Bind ViewModel.IsAdmin, Mode=OneWay, Converter={StaticResource BoolToVisibilityInverseConverter}}" Margin="0,8,0,0">
                        <StackPanel Spacing="16" x:Name="MyKpiPanel">
                            <TextBlock Text="My Performance" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Spacing="4">
                                    <TextBlock Text="Orders" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                    <TextBlock Text="{x:Bind ViewModel.MyKpiOrders, Mode=OneWay}" Style="{StaticResource SubtitleTextBlockStyle}"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Spacing="4">
                                    <TextBlock Text="Revenue" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                    <TextBlock Text="{x:Bind ViewModel.MyKpiRevenue, Mode=OneWay}" Style="{StaticResource SubtitleTextBlockStyle}"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2" Spacing="4">
                                    <TextBlock Text="Rate" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                    <TextBlock Text="{x:Bind ViewModel.MyKpiRate, Mode=OneWay}" Style="{StaticResource SubtitleTextBlockStyle}"/>
                                </StackPanel>
                                <StackPanel Grid.Column="3" Spacing="4">
                                    <TextBlock Text="Commission" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                    <TextBlock Text="{x:Bind ViewModel.MyKpiCommission, Mode=OneWay}" Style="{StaticResource SubtitleTextBlockStyle}" Foreground="Green"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Grid>
                </Grid>
                
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
