<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MyShopClient.Views.Products.ProductDetailView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:MyShopClient.Models"
    xmlns:vm="using:MyShopClient.ViewModels"
    xmlns:helpers="using:MyShopClient.Helpers"
    mc:Ignorable="d">

    <Page.Resources>
        <helpers:BoolToVisibilityConverter x:Key="BoolToVisibleConverter"/>
        <helpers:BoolToVisibilityConverter x:Key="BoolToCollapsedConverter" IsInverted="True"/>
        <helpers:DecimalToDoubleConverter x:Key="DecimalToDoubleConverter"/>
        <helpers:IntToVisibleConverter x:Key="IntToVisibleConverter"/>
        <helpers:IntToDoubleConverter x:Key="IntToDoubleConverter"/>
        <helpers:ImageUrlConverter x:Key="ImageUrlConverter"/>
    </Page.Resources>

    <Grid x:Name="RootGrid" Padding="32" Background="{ThemeResource AppBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" Spacing="16" VerticalAlignment="Center">
                <Button Command="{x:Bind ViewModel.GoBackCommand}" Background="Transparent" BorderThickness="0" Padding="8">
                    <FontIcon Glyph="&#xE72B;" FontFamily="{ThemeResource SymbolThemeFontFamily}" FontSize="18" Foreground="{ThemeResource TextSecondaryBrush}"/>
                </Button>
                <TextBlock Text="Product Detail" FontSize="22" FontWeight="Bold" Foreground="{ThemeResource TextPrimaryBrush}" VerticalAlignment="Center"/>
            </StackPanel>

            <CommandBar Grid.Column="2" Background="Transparent" DefaultLabelPosition="Right">
                <AppBarButton Icon="Edit" Label="Edit" Command="{x:Bind ViewModel.EnableEditCommand}" Visibility="{x:Bind ViewModel.IsEditing, Converter={StaticResource BoolToCollapsedConverter}, Mode=OneWay}"/>
                <AppBarButton Icon="Save" Label="Save" Command="{x:Bind ViewModel.SaveCommand}" Visibility="{x:Bind ViewModel.IsEditing, Converter={StaticResource BoolToVisibleConverter}, Mode=OneWay}"/>
                <AppBarButton Icon="Cancel" Label="Cancel" Command="{x:Bind ViewModel.CancelEditCommand}" Visibility="{x:Bind ViewModel.IsEditing, Converter={StaticResource BoolToVisibleConverter}, Mode=OneWay}"/>
                <AppBarButton Icon="Delete" Label="Delete" Command="{x:Bind ViewModel.DeleteCommand}" Visibility="{x:Bind ViewModel.Product.Id, Converter={StaticResource IntToVisibleConverter}, Mode=OneWay}"/>
            </CommandBar>
        </Grid>

        <!-- Main Content Card -->
        <Border Grid.Row="1" Background="{ThemeResource CardBackgroundBrush}" CornerRadius="16" Padding="24" BorderBrush="{ThemeResource BorderBrush}" BorderThickness="1">
            <Grid>
                <Grid MaxWidth="1200" HorizontalAlignment="Stretch">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Left Column - Form Fields -->
                    <StackPanel Grid.Column="0" Spacing="16">
                        <!-- ID/SKU Row -->
                        <Grid ColumnSpacing="16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBox Header="ID" Text="{x:Bind ViewModel.Product.Id, Mode=OneWay}" IsReadOnly="True" IsEnabled="False"/>
                            <TextBox Grid.Column="1" Header="SKU" Text="{x:Bind ViewModel.Product.Sku, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{x:Bind ViewModel.IsEditing, Mode=OneWay}"/>
                        </Grid>

                        <!-- Name -->
                        <TextBox Header="Name" Text="{x:Bind ViewModel.Product.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{x:Bind ViewModel.IsEditing, Mode=OneWay}"/>

                        <!-- Category -->
                        <ComboBox Header="Category" 
                                  ItemsSource="{x:Bind ViewModel.Categories, Mode=OneWay}"
                                  SelectedItem="{x:Bind ViewModel.SelectedCategory, Mode=TwoWay}"
                                  IsEnabled="{x:Bind ViewModel.IsEditing, Mode=OneWay}"
                                  HorizontalAlignment="Stretch">
                            <ComboBox.ItemTemplate>
                                <DataTemplate x:DataType="models:Category">
                                    <TextBlock Text="{x:Bind Name}"/>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>

                        <!-- Prices -->
                        <Grid ColumnSpacing="16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <NumberBox Header="Import Price" Value="{x:Bind ViewModel.Product.ImportPrice, Mode=TwoWay, Converter={StaticResource DecimalToDoubleConverter}}" IsEnabled="{x:Bind ViewModel.IsEditing, Mode=OneWay}" SpinButtonPlacementMode="Inline"/>
                            <NumberBox Grid.Column="1" Header="Sale Price" Value="{x:Bind ViewModel.Product.SalePrice, Mode=TwoWay, Converter={StaticResource DecimalToDoubleConverter}}" IsEnabled="{x:Bind ViewModel.IsEditing, Mode=OneWay}" SpinButtonPlacementMode="Inline"/>
                        </Grid>

                        <!-- Stock -->
                        <NumberBox Header="Stock" Value="{x:Bind ViewModel.Product.Stock, Mode=TwoWay, Converter={StaticResource IntToDoubleConverter}}" IsEnabled="{x:Bind ViewModel.IsEditing, Mode=OneWay}" SpinButtonPlacementMode="Inline" HorizontalAlignment="Stretch"/>

                        <!-- Description -->
                        <TextBox Header="Description" 
                                 Text="{x:Bind ViewModel.Product.Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                 AcceptsReturn="True" 
                                 Height="80" 
                                 IsEnabled="{x:Bind ViewModel.IsEditing, Mode=OneWay}"/>
                    </StackPanel>

                    <!-- Right Column - Images -->
                    <StackPanel Grid.Column="2" Spacing="12">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                            <TextBlock Text="Images" Style="{ThemeResource BodyStrongTextBlockStyle}" VerticalAlignment="Center"/>
                            <AppBarButton Icon="Add" Label="Add" Command="{x:Bind ViewModel.PickImagesCommand}" Visibility="{x:Bind ViewModel.IsEditing, Converter={StaticResource BoolToVisibleConverter}, Mode=OneWay}" HorizontalAlignment="Right"/>
                        </StackPanel>

                        <ScrollViewer MaxHeight="450" VerticalScrollBarVisibility="Auto">
                            <StackPanel Spacing="8">
                                <!-- Existing Images -->
                                <GridView ItemsSource="{x:Bind ViewModel.Product.Images, Mode=OneWay}" SelectionMode="None">
                                    <GridView.ItemTemplate>
                                        <DataTemplate x:DataType="models:ProductImage">
                                            <Grid Margin="4" Width="80" Height="80" CornerRadius="8" BorderBrush="{ThemeResource BorderBrush}" BorderThickness="1">
                                                <Image Source="{x:Bind Url, Converter={StaticResource ImageUrlConverter}}" Stretch="UniformToFill"/>
                                                <Button Click="DeleteExistingImage_Click" 
                                                        Tag="{x:Bind Id}"
                                                        Loading="DeleteButton_Loading"
                                                        VerticalAlignment="Top" HorizontalAlignment="Right" 
                                                        Background="#D32F2F" Foreground="White" Padding="4" MinWidth="0" Margin="2">
                                                    <FontIcon Glyph="&#xE711;" FontSize="8"/>
                                                </Button>
                                            </Grid>
                                        </DataTemplate>
                                    </GridView.ItemTemplate>
                                </GridView>

                                <!-- Newly Selected Images -->
                                <GridView ItemsSource="{x:Bind ViewModel.SelectedImagePaths, Mode=OneWay}" SelectionMode="None">
                                    <GridView.ItemTemplate>
                                        <DataTemplate x:DataType="x:String">
                                            <Grid Margin="4" Width="80" Height="80" CornerRadius="8" BorderBrush="{ThemeResource BorderBrush}" BorderThickness="1">
                                                <Image Source="{x:Bind Converter={StaticResource ImageUrlConverter}}" Stretch="UniformToFill"/>
                                                <Button Click="RemoveSelectedImage_Click" 
                                                        Tag="{x:Bind}"
                                                        VerticalAlignment="Top" HorizontalAlignment="Right" 
                                                        Background="#D32F2F" Foreground="White" Padding="4" MinWidth="0" Margin="2">
                                                    <FontIcon Glyph="&#xE711;" FontSize="8"/>
                                                </Button>
                                            </Grid>
                                        </DataTemplate>
                                    </GridView.ItemTemplate>
                                </GridView>
                            </StackPanel>
                        </ScrollViewer>
                    </StackPanel>
                </Grid>

                <!-- Loading -->
                <ProgressRing IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}" Width="60" Height="60" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</Page>
