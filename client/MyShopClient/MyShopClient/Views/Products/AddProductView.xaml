<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MyShopClient.Views.Products.AddProductView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:MyShopClient.Models"
    xmlns:helpers="using:MyShopClient.Helpers"
    xmlns:shared="using:MyShopClient.Views.Shared"
    mc:Ignorable="d"
    Background="{ThemeResource AppBackgroundBrush}">
    
    <Page.Resources>
        <helpers:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        
        <!-- Form Label Style -->
        <Style x:Key="FormLabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{ThemeResource TextPrimaryBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>
        
        <!-- Required Indicator Style -->
        <Style x:Key="RequiredIndicatorStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{ThemeResource DangerBrush}"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="2,0,0,0"/>
        </Style>
        
        <!-- Section Title Style -->
        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{ThemeResource TextPrimaryBrush}"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
        </Style>
    </Page.Resources>

    <Grid Padding="24,24,4,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Responsive Visual States -->
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="WideState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="900"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="ContentGrid.ColumnSpacing" Value="24"/>
                        <Setter Target="ImageColumn.Width" Value="*"/>
                        <Setter Target="FormColumn.Width" Value="1.5*"/>
                        <Setter Target="ImageSection.(Grid.Column)" Value="0"/>
                        <Setter Target="ImageSection.(Grid.Row)" Value="0"/>
                        <Setter Target="FormSection.(Grid.Column)" Value="1"/>
                        <Setter Target="FormSection.(Grid.Row)" Value="0"/>
                    </VisualState.Setters>
                </VisualState>

                <VisualState x:Name="NarrowState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="ContentGrid.ColumnSpacing" Value="0"/>
                        <Setter Target="ContentGrid.RowSpacing" Value="24"/>
                        <Setter Target="ImageColumn.Width" Value="*"/>
                        <Setter Target="FormColumn.Width" Value="0"/>
                        <Setter Target="ImageSection.(Grid.Column)" Value="0"/>
                        <Setter Target="ImageSection.(Grid.Row)" Value="0"/>
                        <Setter Target="FormSection.(Grid.Column)" Value="0"/>
                        <Setter Target="FormSection.(Grid.Row)" Value="1"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="16" Margin="0,0,0,24">
            <Button 
                x:Name="BackButton"
                Click="BackButton_Click"
                Style="{ThemeResource AccentButtonStyle}"
                Background="{ThemeResource CardBackgroundBrush}"
                BorderBrush="{ThemeResource BorderBrush}"
                Padding="12,8">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE72B;" FontSize="16" Foreground="{ThemeResource TextPrimaryBrush}"/>
                    <TextBlock Text="Back" Foreground="{ThemeResource TextPrimaryBrush}"/>
                </StackPanel>
            </Button>
            
            <TextBlock 
                Text="{x:Bind ViewModel.PageTitle, Mode=OneWay}" 
                FontSize="24" 
                FontWeight="Bold"
                Foreground="{ThemeResource TextPrimaryBrush}"
                VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Main Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="0,0,20,0">
            <Grid x:Name="ContentGrid" ColumnSpacing="24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition x:Name="ImageColumn" Width="*"/>
                    <ColumnDefinition x:Name="FormColumn" Width="1.5*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Form Section -->
                <StackPanel x:Name="FormSection" Grid.Column="1" Grid.Row="0" Spacing="24">
                    
                    <!-- Basic Information Card -->
                    <Border 
                        Background="{ThemeResource CardBackgroundBrush}" 
                        CornerRadius="16"
                        BorderBrush="{ThemeResource BorderBrush}"
                        BorderThickness="1"
                        Padding="24">
                        <StackPanel Spacing="16">
                            <TextBlock Text="Basic Information" Style="{StaticResource SectionTitleStyle}"/>
                            
                            <!-- Product Name -->
                            <StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Product Name" Style="{StaticResource FormLabelStyle}"/>
                                    <TextBlock Text="*" Style="{StaticResource RequiredIndicatorStyle}"/>
                                </StackPanel>
                                <TextBox 
                                    PlaceholderText="Enter product name..."
                                    Text="{x:Bind ViewModel.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                    HorizontalAlignment="Stretch"/>
                            </StackPanel>
                            
                            <!-- SKU -->
                            <StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="SKU" Style="{StaticResource FormLabelStyle}"/>
                                    <TextBlock Text="*" Style="{StaticResource RequiredIndicatorStyle}"/>
                                </StackPanel>
                                <TextBox 
                                    PlaceholderText="e.g., PRD-001"
                                    Text="{x:Bind ViewModel.Sku, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                    HorizontalAlignment="Stretch"/>
                            </StackPanel>
                            
                            <!-- Description -->
                            <StackPanel>
                                <TextBlock Text="Description" Style="{StaticResource FormLabelStyle}"/>
                                <TextBox 
                                    PlaceholderText="Enter product description..."
                                    Text="{x:Bind ViewModel.Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                    TextWrapping="Wrap"
                                    AcceptsReturn="True"
                                    Height="100"
                                    HorizontalAlignment="Stretch"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    
                    <!-- Pricing Card -->
                    <Border 
                        Background="{ThemeResource CardBackgroundBrush}" 
                        CornerRadius="16"
                        BorderBrush="{ThemeResource BorderBrush}"
                        BorderThickness="1"
                        Padding="24">
                        <StackPanel Spacing="16">
                            <TextBlock Text="Pricing" Style="{StaticResource SectionTitleStyle}"/>
                            
                            <Grid ColumnSpacing="16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- Import Price -->
                                <StackPanel Grid.Column="0">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="Import Price (VND)" Style="{StaticResource FormLabelStyle}"/>
                                        <TextBlock Text="*" Style="{StaticResource RequiredIndicatorStyle}"/>
                                    </StackPanel>
                                    <NumberBox 
                                        PlaceholderText="0"
                                        Value="{x:Bind ViewModel.ImportPrice, Mode=TwoWay}"
                                        Minimum="0"
                                        SpinButtonPlacementMode="Compact"
                                        HorizontalAlignment="Stretch"/>
                                </StackPanel>
                                
                                <!-- Sale Price -->
                                <StackPanel Grid.Column="1">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="Sale Price (VND)" Style="{StaticResource FormLabelStyle}"/>
                                        <TextBlock Text="*" Style="{StaticResource RequiredIndicatorStyle}"/>
                                    </StackPanel>
                                    <NumberBox 
                                        PlaceholderText="0"
                                        Value="{x:Bind ViewModel.SalePrice, Mode=TwoWay}"
                                        Minimum="0"
                                        SpinButtonPlacementMode="Compact"
                                        HorizontalAlignment="Stretch"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>
                    
                    <!-- Inventory Card -->
                    <Border 
                        Background="{ThemeResource CardBackgroundBrush}" 
                        CornerRadius="16"
                        BorderBrush="{ThemeResource BorderBrush}"
                        BorderThickness="1"
                        Padding="24">
                        <StackPanel Spacing="16">
                            <TextBlock Text="Inventory" Style="{StaticResource SectionTitleStyle}"/>
                            
                            <Grid ColumnSpacing="16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- Stock -->
                                <StackPanel Grid.Column="0">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="Stock Quantity" Style="{StaticResource FormLabelStyle}"/>
                                        <TextBlock Text="*" Style="{StaticResource RequiredIndicatorStyle}"/>
                                    </StackPanel>
                                    <NumberBox 
                                        PlaceholderText="0"
                                        Value="{x:Bind ViewModel.Stock, Mode=TwoWay}"
                                        Minimum="0"
                                        SpinButtonPlacementMode="Compact"
                                        HorizontalAlignment="Stretch"/>
                                </StackPanel>
                                
                                <!-- Category -->
                                <StackPanel Grid.Column="1">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="Category" Style="{StaticResource FormLabelStyle}"/>
                                        <TextBlock Text="*" Style="{StaticResource RequiredIndicatorStyle}"/>
                                    </StackPanel>
                                    <ComboBox 
                                        PlaceholderText="Select category"
                                        ItemsSource="{x:Bind ViewModel.Categories, Mode=OneWay}"
                                        SelectedItem="{x:Bind ViewModel.SelectedCategory, Mode=TwoWay}"
                                        DisplayMemberPath="Name"
                                        HorizontalAlignment="Stretch"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>
                    
                    <!-- Action Buttons -->
                    <StackPanel Orientation="Horizontal" Spacing="12" HorizontalAlignment="Left">
                        <Button 
                            Click="SaveButton_Click"
                            Style="{ThemeResource AccentButtonStyle}" 
                            Padding="24,12">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE74E;" FontSize="16"/>
                                <TextBlock Text="{x:Bind ViewModel.SaveButtonText, Mode=OneWay}"/>
                            </StackPanel>
                        </Button>
                        
                        <Button 
                            Click="CancelButton_Click"
                            Style="{ThemeResource SecondaryButtonStyle}" 
                            Padding="24,12">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE711;" FontSize="16"/>
                                <TextBlock Text="Cancel"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </StackPanel>

                <!-- Image Section -->
                <Border 
                    x:Name="ImageSection"
                    Grid.Column="0" 
                    Grid.Row="0"
                    Background="{ThemeResource CardBackgroundBrush}" 
                    CornerRadius="16"
                    BorderBrush="{ThemeResource BorderBrush}"
                    BorderThickness="1"
                    Padding="24">
                    <StackPanel Spacing="16">
                        <TextBlock Text="Product Images" Style="{StaticResource SectionTitleStyle}"/>
                        
                        <!-- Drop Zone -->
                        <Border
                            x:Name="DropZone"
                            Background="{ThemeResource CardBackgroundBrush}"
                            BorderBrush="{ThemeResource BorderBrush}"
                            BorderThickness="2"
                            CornerRadius="12"
                            Padding="32"
                            AllowDrop="True"
                            DragOver="DropZone_DragOver"
                            DragLeave="DropZone_DragLeave"
                            Drop="DropZone_Drop"
                            PointerPressed="DropZone_PointerPressed">
                            <StackPanel HorizontalAlignment="Center" Spacing="12">
                                <FontIcon 
                                    Glyph="&#xE898;" 
                                    FontSize="48" 
                                    Foreground="{ThemeResource TextSecondaryBrush}"
                                    HorizontalAlignment="Center"/>
                                <TextBlock 
                                    Text="Drag &amp; drop images here" 
                                    Foreground="{ThemeResource TextSecondaryBrush}"
                                    HorizontalAlignment="Center"
                                    FontSize="14"
                                    FontWeight="SemiBold"/>
                                <TextBlock 
                                    Text="or click to browse" 
                                    Foreground="{ThemeResource TextSecondaryBrush}"
                                    HorizontalAlignment="Center"
                                    FontSize="12"/>
                                <TextBlock 
                                    Text="Supported: JPG, PNG, GIF, BMP, WEBP" 
                                    Foreground="{ThemeResource TextSecondaryBrush}"
                                    HorizontalAlignment="Center"
                                    FontSize="11"
                                    Opacity="0.7"/>
                            </StackPanel>
                        </Border>
                        
                        <!-- Selected Images Gallery -->
                        <GridView
                            x:Name="ImagesGridView"
                            ItemsSource="{x:Bind ViewModel.ProductImages, Mode=OneWay}"
                            Visibility="{x:Bind ViewModel.HasImages, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
                            SelectionMode="None"
                            IsItemClickEnabled="False"
                            Padding="0">
                            
                            <GridView.ItemTemplate>
                                <DataTemplate x:DataType="models:LocalProductImage">
                                    <Grid Width="100" Height="100" Margin="4">
                                        <Border 
                                            CornerRadius="8" 
                                            Background="{ThemeResource SystemChromeLowColor}">
                                            <Image 
                                                Source="{x:Bind DisplaySource}" 
                                                Stretch="UniformToFill"/>
                                        </Border>
                                        
                                        <!-- Remove Button -->
                                        <Button 
                                            HorizontalAlignment="Right" 
                                            VerticalAlignment="Top"
                                            Margin="0,-4,-4,0"
                                            Padding="4"
                                            Width="24"
                                            Height="24"
                                            CornerRadius="12"
                                            Background="{ThemeResource DangerBrush}"
                                            BorderThickness="0"
                                            Click="RemoveImage_Click"
                                            Tag="{x:Bind}">
                                            <FontIcon 
                                                Glyph="&#xE711;" 
                                                FontSize="10" 
                                                Foreground="White"/>
                                        </Button>
                                        
                                        <!-- File Name Tooltip -->
                                        <ToolTipService.ToolTip>
                                            <ToolTip Content="{x:Bind FileName}"/>
                                        </ToolTipService.ToolTip>
                                    </Grid>
                                </DataTemplate>
                            </GridView.ItemTemplate>
                        </GridView>
                        
                        <TextBlock 
                            Text="No images selected yet"
                            Foreground="{ThemeResource TextSecondaryBrush}"
                            FontSize="12"
                            HorizontalAlignment="Center"
                            Visibility="{x:Bind ViewModel.HasImages, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Inverse}"/>
                    </StackPanel>
                </Border>
            </Grid>
        </ScrollViewer>
        
        <!-- Loading Overlay -->
        <Grid 
            Visibility="{x:Bind ViewModel.IsSaving, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
            Background="{ThemeResource LayerOnMicaBaseAltFillColorDefaultBrush}">
            <StackPanel 
                HorizontalAlignment="Center" 
                VerticalAlignment="Center"
                Spacing="16">
                <ProgressRing IsActive="{x:Bind ViewModel.IsSaving, Mode=OneWay}" Width="48" Height="48"/>
                <TextBlock 
                    Text="Saving product..." 
                    Foreground="{ThemeResource TextSecondaryBrush}"
                    HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <!-- Slide Notification Component -->
        <shared:SlideNotification 
            x:Name="Notification" 
            Grid.RowSpan="2" 
            Canvas.ZIndex="999"/>
    </Grid>
</Page>
