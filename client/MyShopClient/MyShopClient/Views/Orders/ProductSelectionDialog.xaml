<ContentDialog
    x:Class="MyShopClient.Views.Orders.ProductSelectionDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyShopClient.Views.Orders"
    xmlns:vm="using:MyShopClient.ViewModels"
    xmlns:api="using:MyShopClient.Services.Api"
    xmlns:models="using:MyShopClient.Models"
    xmlns:helpers="using:MyShopClient.Helpers"
    Title="Select Product"
    PrimaryButtonText="Add to Order"
    SecondaryButtonText="Cancel"
    DefaultButton="Primary"
    IsPrimaryButtonEnabled="{x:Bind ViewModel.SelectedProduct, Mode=OneWay, Converter={StaticResource NullToBoolConverter}}"
    Style="{StaticResource DefaultContentDialogStyle}"
    RequestedTheme="{x:Bind RequestedTheme, Mode=OneWay}">

    <ContentDialog.Resources>
        <helpers:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <helpers:NullToBoolConverter x:Key="NullToBoolConverter"/>
        <helpers:BoolToPaginationBrushConverter x:Key="BoolToPaginationBrushConverter"/>
        <helpers:StockToStringConverter x:Key="StockToStringConverter"/>
        <helpers:ProductImageToSourceConverter x:Key="ProductImageToSourceConverter"/>
        
        <!-- Table Header Style -->
        <Style x:Key="TableHeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{ThemeResource TextSecondaryBrush}"/>
            <Setter Property="CharacterSpacing" Value="50"/>
        </Style>
    </ContentDialog.Resources>

    <Grid Width="500" MaxHeight="500">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Search Bar -->
        <AutoSuggestBox 
            Grid.Row="0"
            PlaceholderText="Search products by name, SKU..."
            Text="{x:Bind ViewModel.SearchKeyword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            QueryIcon="Find"
            Width="300"
            HorizontalAlignment="Left"
            Margin="0,0,0,16"/>

        <!-- Loading -->
        <ProgressBar 
            Grid.Row="1" 
            IsIndeterminate="True" 
            Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>

        <!-- Product List + Pagination in ScrollViewer -->
        <ScrollViewer 
            Grid.Row="2"
            VerticalScrollBarVisibility="Auto"
            HorizontalScrollBarVisibility="Auto"
            Padding="0,0,8,0">
            <StackPanel>
                <ListView
                    ItemsSource="{x:Bind ViewModel.Products, Mode=OneWay}"
                    SelectedItem="{x:Bind ViewModel.SelectedProduct, Mode=TwoWay}"
                    SelectionMode="Single">
                    <ListView.Header>
                        <Grid Background="{ThemeResource SystemChromeLowColor}" Padding="12,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*" MinWidth="150"/>
                                <ColumnDefinition Width="*" MinWidth="80"/>
                                <ColumnDefinition Width="*" MinWidth="60"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="PRODUCT" Style="{StaticResource TableHeaderTextStyle}"/>
                            <TextBlock Grid.Column="1" Text="PRICE" VerticalAlignment="Center" Style="{StaticResource TableHeaderTextStyle}"/>
                            <TextBlock Grid.Column="2" Text="STOCK" VerticalAlignment="Center" Style="{StaticResource TableHeaderTextStyle}"/>
                        </Grid>
                    </ListView.Header>
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:ApiProduct">
                            <Grid Padding="12,10" BorderBrush="{ThemeResource BorderBrush}" BorderThickness="0,0,0,1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="2*" MinWidth="150"/>
                                    <ColumnDefinition Width="*" MinWidth="80"/>
                                    <ColumnDefinition Width="*" MinWidth="60"/>
                                </Grid.ColumnDefinitions>

                                <!-- Product Info -->
                                <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="10">
                                    <Border Width="40" Height="40" CornerRadius="4" Background="#f0f0f0">
                                        <Image Source="{x:Bind Images, Converter={StaticResource ProductImageToSourceConverter}}" Stretch="UniformToFill"/>
                                    </Border>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold" Foreground="{ThemeResource TextPrimaryBrush}" TextTrimming="CharacterEllipsis" MaxWidth="150"/>
                                        <TextBlock Text="{x:Bind Sku}" FontSize="11" Foreground="{ThemeResource TextSecondaryBrush}"/>
                                    </StackPanel>
                                </StackPanel>

                                <!-- Price -->
                                <TextBlock Grid.Column="1" Text="{x:Bind FormattedPrice}" VerticalAlignment="Center" Foreground="{ThemeResource TextPrimaryBrush}" FontWeight="SemiBold" FontSize="13"/>

                                <!-- Stock -->
                                <TextBlock Grid.Column="2" Text="{x:Bind Stock}" VerticalAlignment="Center" Foreground="{ThemeResource TextSecondaryBrush}" FontSize="13"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <!-- Pagination -->
                <Grid Margin="0,16,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" VerticalAlignment="Center" Foreground="{ThemeResource TextSecondaryBrush}" FontSize="12">
                        <Run Text="Page"/>
                        <Run Text="{x:Bind ViewModel.CurrentPage, Mode=OneWay}" Foreground="{ThemeResource TextPrimaryBrush}"/>
                        <Run Text="of"/>
                        <Run Text="{x:Bind ViewModel.TotalPages, Mode=OneWay}" Foreground="{ThemeResource TextPrimaryBrush}"/>
                        <Run Text="("/>
                        <Run Text="{x:Bind ViewModel.TotalItems, Mode=OneWay}" Foreground="{ThemeResource TextPrimaryBrush}"/>
                        <Run Text="products)"/>
                    </TextBlock>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="4">
                        <!-- Previous Button -->
                        <Button 
                            Command="{x:Bind ViewModel.PreviousPageCommand}"
                            IsEnabled="{x:Bind ViewModel.CanGoPrev, Mode=OneWay}"
                            Style="{StaticResource PaginationButtonStyle}">
                            <FontIcon Glyph="&#xE76B;" FontSize="12"/>
                        </Button>
                        
                        <!-- Page Number Buttons -->
                        <ItemsControl ItemsSource="{x:Bind ViewModel.Pages, Mode=OneWay}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" Spacing="4"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="vm:PageInfo">
                                    <Button 
                                        Content="{x:Bind Text}"
                                        Command="{x:Bind Command}"
                                        IsEnabled="{x:Bind IsEnabled}"
                                        Style="{StaticResource PaginationButtonStyle}"
                                        Background="{x:Bind IsCurrent, Converter={StaticResource BoolToPaginationBrushConverter}}"
                                        Foreground="{x:Bind IsCurrent, Converter={StaticResource BoolToPaginationBrushConverter}, ConverterParameter=Foreground}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        
                        <!-- Next Button -->
                        <Button 
                            Command="{x:Bind ViewModel.NextPageCommand}"
                            IsEnabled="{x:Bind ViewModel.CanGoNext, Mode=OneWay}"
                            Style="{StaticResource PaginationButtonStyle}">
                            <FontIcon Glyph="&#xE76C;" FontSize="12"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</ContentDialog>
