<Page
    x:Class="MyShopClient.Views.Customers.CustomerDetailView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyShopClient.Views.Customers"
    xmlns:viewmodels="using:MyShopClient.ViewModels"
    xmlns:helpers="using:MyShopClient.Helpers"
    xmlns:shared="using:MyShopClient.Views.Shared"
    Background="{ThemeResource AppBackgroundBrush}">
    
    <Page.Resources>
        <helpers:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Page.Resources>

    <Grid Padding="24,24,4,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Responsive Visual States -->
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="WideState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="900"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="ContentGrid.ColumnSpacing" Value="24"/>
                        <Setter Target="CustomerInfoColumn.Width" Value="*"/>
                        <Setter Target="OrderHistoryColumn.Width" Value="*"/>
                        <Setter Target="CustomerInfoSection.(Grid.Column)" Value="0"/>
                        <Setter Target="CustomerInfoSection.(Grid.Row)" Value="0"/>
                        <Setter Target="OrderHistorySection.(Grid.Column)" Value="1"/>
                        <Setter Target="OrderHistorySection.(Grid.Row)" Value="0"/>
                    </VisualState.Setters>
                </VisualState>

                <VisualState x:Name="NarrowState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="ContentGrid.ColumnSpacing" Value="0"/>
                        <Setter Target="ContentGrid.RowSpacing" Value="24"/>
                        <Setter Target="CustomerInfoColumn.Width" Value="*"/>
                        <Setter Target="OrderHistoryColumn.Width" Value="0"/>
                        <Setter Target="CustomerInfoSection.(Grid.Column)" Value="0"/>
                        <Setter Target="CustomerInfoSection.(Grid.Row)" Value="0"/>
                        <Setter Target="OrderHistorySection.(Grid.Column)" Value="0"/>
                        <Setter Target="OrderHistorySection.(Grid.Row)" Value="1"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="16" Margin="0,0,0,24">
            <Button 
                x:Name="BackButton"
                Click="BackButton_Click"
                Style="{ThemeResource AccentButtonStyle}"
                Background="{ThemeResource CardBackgroundBrush}"
                BorderBrush="{ThemeResource BorderBrush}"
                Padding="12,8">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE72B;" FontSize="16" Foreground="{ThemeResource TextPrimaryBrush}"/>
                    <TextBlock Text="Back" Foreground="{ThemeResource TextPrimaryBrush}"/>
                </StackPanel>
            </Button>
            
            <TextBlock 
                Text="Customer Details" 
                FontSize="24" 
                FontWeight="Bold"
                Foreground="{ThemeResource TextPrimaryBrush}"
                VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Main Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="0,0,20,0">
            <Grid x:Name="ContentGrid" ColumnSpacing="24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition x:Name="CustomerInfoColumn" Width="*"/>
                    <ColumnDefinition x:Name="OrderHistoryColumn" Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Customer Info Section -->
                <StackPanel x:Name="CustomerInfoSection" Grid.Column="0" Grid.Row="0" Spacing="16">
                    
                    <!-- Customer Profile Card -->
                    <Border 
                        Background="{ThemeResource CardBackgroundBrush}" 
                        CornerRadius="16"
                        BorderBrush="{ThemeResource BorderBrush}"
                        BorderThickness="1"
                        Padding="24">
                        <StackPanel Spacing="20">
                            <!-- Avatar and Name -->
                            <StackPanel Orientation="Horizontal" Spacing="20">
                                <Ellipse Width="80" Height="80">
                                    <Ellipse.Fill>
                                        <ImageBrush ImageSource="{x:Bind ViewModel.AvatarUrl, Mode=OneWay}"/>
                                    </Ellipse.Fill>
                                </Ellipse>
                                
                                <StackPanel VerticalAlignment="Center" Spacing="4">
                                    <TextBlock 
                                        Text="{x:Bind ViewModel.Name, Mode=OneWay}" 
                                        FontSize="24" 
                                        FontWeight="Bold" 
                                        Foreground="{ThemeResource TextPrimaryBrush}"/>
                                    <TextBlock 
                                        Text="{x:Bind ViewModel.FormattedCreatedDate, Mode=OneWay}" 
                                        FontSize="14" 
                                        Foreground="{ThemeResource TextSecondaryBrush}"/>
                                </StackPanel>
                            </StackPanel>
                            
                            <Rectangle Height="1" Fill="{ThemeResource BorderBrush}"/>
                            
                            <!-- Contact Info -->
                            <StackPanel Spacing="16">
                                <StackPanel Orientation="Horizontal" Spacing="12">
                                    <FontIcon Glyph="&#xE715;" FontSize="18" Foreground="{ThemeResource TextSecondaryBrush}" Width="24"/>
                                    <StackPanel>
                                        <TextBlock Text="Email" FontSize="12" Foreground="{ThemeResource TextSecondaryBrush}"/>
                                        <TextBlock Text="{x:Bind ViewModel.Email, Mode=OneWay}" Foreground="{ThemeResource TextPrimaryBrush}"/>
                                    </StackPanel>
                                </StackPanel>
                                
                                <StackPanel Orientation="Horizontal" Spacing="12">
                                    <FontIcon Glyph="&#xE717;" FontSize="18" Foreground="{ThemeResource TextSecondaryBrush}" Width="24"/>
                                    <StackPanel>
                                        <TextBlock Text="Phone" FontSize="12" Foreground="{ThemeResource TextSecondaryBrush}"/>
                                        <TextBlock Text="{x:Bind ViewModel.Phone, Mode=OneWay}" Foreground="{ThemeResource TextPrimaryBrush}"/>
                                    </StackPanel>
                                </StackPanel>
                                
                                <StackPanel Orientation="Horizontal" Spacing="12">
                                    <FontIcon Glyph="&#xE707;" FontSize="18" Foreground="{ThemeResource TextSecondaryBrush}" Width="24"/>
                                    <StackPanel>
                                        <TextBlock Text="Address" FontSize="12" Foreground="{ThemeResource TextSecondaryBrush}"/>
                                        <TextBlock Text="{x:Bind ViewModel.Address, Mode=OneWay}" Foreground="{ThemeResource TextPrimaryBrush}" TextWrapping="Wrap"/>
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    
                    <!-- Action Buttons -->
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <Button Style="{ThemeResource AccentButtonStyle}" Padding="20,10" Click="EditCustomerButton_Click">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE70F;" FontSize="14"/>
                                <TextBlock Text="Edit Customer"/>
                            </StackPanel>
                        </Button>
                        
                        <Button Background="{ThemeResource CardBackgroundBrush}" BorderBrush="#EF4444" Padding="20,10" Click="DeleteCustomerButton_Click">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE74D;" FontSize="14" Foreground="#EF4444"/>
                                <TextBlock Text="Delete" Foreground="#EF4444"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </StackPanel>

                <!-- Order History Section -->
                <Border 
                    x:Name="OrderHistorySection"
                    Grid.Column="1"
                    Grid.Row="0"
                    Background="{ThemeResource CardBackgroundBrush}" 
                    CornerRadius="16"
                    BorderBrush="{ThemeResource BorderBrush}"
                    BorderThickness="1"
                    Padding="24">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <!-- Header -->
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="12" Margin="0,0,0,16">
                            <TextBlock Text="Order History" FontSize="18" FontWeight="Bold" Foreground="{ThemeResource TextPrimaryBrush}"/>
                            <Border Background="{ThemeResource PrimaryBrush}" CornerRadius="12" Padding="8,4">
                                <TextBlock Text="{x:Bind ViewModel.OrderCount, Mode=OneWay}" FontSize="12" FontWeight="SemiBold" Foreground="White"/>
                            </Border>
                        </StackPanel>
                        
                        <!-- Orders List -->
                        <ListView 
                            Grid.Row="1"
                            ItemsSource="{x:Bind ViewModel.RecentOrders, Mode=OneWay}"
                            SelectionMode="None">
                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                    <Setter Property="Padding" Value="0"/>
                                    <Setter Property="Margin" Value="0,0,0,12"/>
                                </Style>
                            </ListView.ItemContainerStyle>
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="viewmodels:CustomerOrderViewModel">
                                    <Grid Padding="12" Background="{ThemeResource SystemChromeLowColor}" CornerRadius="8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                            <TextBlock Text="{x:Bind OrderId}" FontWeight="SemiBold" Foreground="{ThemeResource PrimaryBrush}"/>
                                            <TextBlock Text="{x:Bind FormattedDate}" FontSize="12" Foreground="{ThemeResource TextSecondaryBrush}"/>
                                        </StackPanel>
                                        
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right">
                                            <TextBlock Text="{x:Bind FormattedAmount}" FontWeight="SemiBold" Foreground="{ThemeResource TextPrimaryBrush}" HorizontalAlignment="Right"/>
                                            <Border Background="{x:Bind StatusBackground}" CornerRadius="6" Padding="8,2" HorizontalAlignment="Right">
                                                <TextBlock Text="{x:Bind Status}" FontSize="11" Foreground="{x:Bind StatusForeground}"/>
                                            </Border>
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Grid>
                </Border>
            </Grid>
        </ScrollViewer>
        
        <!-- Loading Overlay -->
        <Grid 
            Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
            Background="{ThemeResource LayerOnMicaBaseAltFillColorDefaultBrush}">
            <StackPanel 
                HorizontalAlignment="Center" 
                VerticalAlignment="Center"
                Spacing="16">
                <ProgressRing IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}" Width="48" Height="48"/>
                <TextBlock 
                    Text="Loading customer details..." 
                    Foreground="{ThemeResource TextSecondaryBrush}"
                    HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <!-- Slide Notification Component -->
        <shared:SlideNotification 
            x:Name="Notification" 
            Grid.RowSpan="2" 
            Canvas.ZIndex="999"/>
    </Grid>
</Page>

